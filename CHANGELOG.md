## [Unreleased]

* Add `--monitor`/`-M` flag to allow specifying which monitor to display the choose GUI. Supports detection by index, display ID, or name. Defaults to main monitor if not specified or invalid.