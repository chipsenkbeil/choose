## OPTIONS

-M *monitor*

: Open choose’s GUI on the specified monitor/display. The argument can be:
    - An integer index (e.g., 0 for main, 1 for secondary)
    - The display ID (as shown by system utilities)
    - The display’s name (as shown in Display preferences)
  Defaults to the main window if not provided or invalid.
